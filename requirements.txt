# Document-analysis workspace dependencies
# Each analysis task lives on its own branch ("separate space"); see AGENTS.md.

# PDF parsing / text & layout extraction
PyMuPDF==1.24.10
pdfplumber==0.11.4

# Word documents
python-docx==1.1.2

# Tabular data + spreadsheet I/O
pandas==2.2.3
openpyxl==3.1.5
